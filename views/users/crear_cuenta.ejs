<!DOCTYPE html>
<html>
    <head>
        <%- include('../partials/head') %>
    </head>

    <body id="fondobody">

        <header id="header">
            <%- include('../partials/header') %>
        </header>

        <main id="fondomain">
            <br>
                <h1 id="titulo">  CREAR CUENTA </h1>
                <div id=container>

                 <!--   <% if (locals && locals.errors) { %>
                        <% errors.forEach(error => { %>
                        <% if (error.param=="email") { %>
                          <h3> <%= error.msg%>  </h3> 
                        <% } %>
                      <% }) %> 
                    <% } %> -->
                    

                <form id="form" action="/users/crear_cuenta" method="POST" >
                    <br>
                    <div class="formulario__grupo-input">
                      <input class="formulario" type="text" placeholder="Nombre" name="name" id="nombre"> </input>
                      <i id="name_check" class="formulario__validacion-estado fas fa-times-circle"></i>
                    </div>
                    <p id="name_msg">El nombre tiene que tener de 4 a 16 dígitos y solo puede contener numeros, letras y guion bajo.</p>
		                
                    <% if (locals && locals.errors) { %>
                        <% errors.forEach(error => { %>
                        <% if (error.param=="name") { %>
                          <h3> <%= error.msg%>  </h3> 
                        <% } %>
                      <% }) %> 
                    <% } %> 
                    <br>
                    <div class="formulario__grupo-input">
                    <input class="formulario" type="text" placeholder="Apellido" name="lastName" id="last-nombre"> </input>
                    <i id="lastName_check" class="formulario__validacion-estado fas fa-times-circle"></i>
                  </div>
                  <p id="lastName_msg">El apellido tiene que ser de 4 a 16 dígitos y solo puede contener numeros, letras y guion bajo.</p>
                    <% if (locals && locals.errors) { %>
                        <% errors.forEach(error => { %>
                        <% if (error.param=="lastName") { %>
                          <h5> <%= error.msg%>  </h5> 
                        <% } %>
                      <% }) %> 
                    <% } %> 
                    <br>
                    <div class="formulario__grupo-input">
                      <input class="formulario" type="text" placeholder="Email" name="email" id="email"> </input>
                      <i id="email_check" class="formulario__validacion-estado fas fa-times-circle"></i>
                    </div>
                    <p id="email_msg">El email solo puede contener letras, numeros, puntos, guiones y guion bajo.</p>
                      <% if (locals && locals.errors) { %>
                        <% errors.forEach(error => { %>
                          <% if (error.param=="email") { %>
                          <h6> <%= error.msg%>  </h6> 
                          <% } %>
                        <% }) %> 
                      <% } %> 
                    <br>
                    <div class="formulario__grupo-input">
                      <input class="formulario" type="text" placeholder= "Telefono (opcional)" name="telephone" id="telefono"> </input>
                      <i id="telephone_check" class="formulario__validacion-estado fas fa-times-circle"></i>
                    </div>
                    <p id="telephone_msg">El telefono solo puede contener numeros y el maximo son 14 dígitos.</p>
                    
                    <br>
                    <div class="formulario__grupo-input">
                    <input class="formulario" type="password" placeholder="Contraseña"  name="password" id="contraseña"> </input>
                    <i id="password_check" class="formulario__validacion-estado fas fa-times-circle"></i>
                    </div>
                    <p id="password_msg">La contraseña tiene que ser de 4 a 12 dígitos.</p>

                    <% if (locals && locals.errors) { %>
                        <% errors.forEach(error => { %>
                        <% if (error.param=="password") { %>
                          <h4> <%= error.msg%>  </h4> 
                        <% } %>
                      <% }) %> 
                    <% } %> 
                    <br>
                    <input class="formulario" type="file" placeholder="perfil" name="image" id="perfil"> </input>
                    <label for="imageAvatar" class="form-label">Seleccionar Imagen:</label>             
                    <br>
                    
                    <div class="formulario__mensaje" id="formulario__mensaje">
                      <i class="fas fa-exclamation-triangle"></i> <b>Error: Por favor rellena el formulario correctamente.</b>
                    </div>

                    <div class="boton-container">
                    <button type= "submit" class="botoncito"> Crear cuenta </button>
                    </div>
                    <div>
                    <h5 class="formulario__mensaje-exito" id="formulario__mensaje-exito">Formulario enviado exitosamente!</h5>
                    </div>
                    <br>
                    <br>
                    <a href="/users/login"><label id=parrafo2>¿Tiene una cuenta?</label> <button type="button" class="botoncito"> Iniciar sesión </button></a>
                </form>
            </div>     
        </main>
        <hr class="linea">

        <footer class="footer">
            <%- include ('../partials/footer') %>
        </footer> 
        <script src="../js/crear_cuenta.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
        
	      <script src="https://kit.fontawesome.com/2c36e9b7b1.js" crossorigin="anonymous"></script> 
    </body>
</html>
